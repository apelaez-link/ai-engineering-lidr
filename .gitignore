# Secretos — NUNCA subir el .env real al repositorio
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Entornos virtuales (uv crea .venv)
.venv/
venv/

# uv
.uv/

# IDE / SO
.vscode/
.idea/
.DS_Store

# Stress test (sesión 06): PDFs sintéticos generados por build_pdfs.py.
# Son binarios grandes y deterministas: se regeneran con el script, no se versionan.
evals/stress/fixtures/*.pdf
# Nota: evals/stress/results.csv SÍ se versiona como MUESTRA (modo mock), junto con
# REPORT.md, para que el lector vea el formato del deliverable sin ejecutar nada.
