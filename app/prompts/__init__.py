"""Paquete de prompts: templates Jinja2 versionados para el estimador.

La estructura interna es:
    prompts/
        loader.py            <- función principal de renderizado
        estimation/
            v1/              <- primera versión del template (baseline)
            v2/              <- variación deliberada (tono más conciso)
"""
